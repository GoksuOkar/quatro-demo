export default function Login() {
  return (
    // do a call with the cookie to see if already logged in, changes auth token
    // or login and change the auth token
    <div>
      login
    </div>
  )
}